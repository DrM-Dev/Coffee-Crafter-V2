this code is standalone and doesn't reqire importing packages outside the built-in packages in python 
<!> every prompt posibility is mentioned
<!> every user-input is error-proofed with correction feedback 
<!> the default-stock inside the virtual coffe machine can be re-filled by user input
:] 
_____________________________
Commands are:
1-to choose a drink from the menu type the name of the item:
"espresso" "latte" "cappuccino"

2-"help"
For input options type

3-"Report"
To check the resources inside the machine

4-"Restock" 
To restock the machine supplies, follow the given instructions on the label under the lid.

5-"Off" 
To turn off the machine, followed by conformation "y/yes".
